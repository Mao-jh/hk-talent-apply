---
name: hk-talent-apply
description: 优才高才申请·获批画像预审（按次收费，4.99 元/次）。当用户问「香港优才申请条件我够不够」「高才A类收入怎么算」「理财收益能算250万收入吗」「自评过了会被拒吗」「优才高才要准备什么材料」「中介说包装收入靠谱吗」时使用。输入拟申请计划（高才A/B/C/优才）与个人画像（学历院校、收入结构与完税、工作年限与雇佣形态、中介参与），返回获批风险报告：风险等级与置信度、逐条风险点（入境处执行口径条目编号+相似获批/拒批案例编号+补强建议）、四类计划申请要求对照、申请必知（A类收入双轨时间算法、B/C类只认本科榜单、优才12准则满足6项≠获批）、拒批与造假后果。
runtime: remote http
endpoint: https://airpriv.top/api/hk-talent-apply
cost: 4.99 CNY / per_call
---

# 优才高才申请·获批画像预审（集成支付版）

本 Skill 是支付宝 AI 收「自研商家」架构的**薄壳**：本地只做编排与转发，
支付与履约全部由后端完成，**不持有私钥、不下单、不碰资金**——
支付全程走支付宝官方 `alipay-bot` CLI 与官方收银台，信任面干净。

**本 Skill 已内嵌完整支付能力**：无需用户另行安装任何官方支付 Skill。

## 工作模型

先确定当前状态，再按状态行动：

| 当前状态 | 下一步 |
|---|---|
| 用户问优才/高才申请资格、收入口径、材料（未发起请求） | 按「调用前必采字段」收集画像，然后按「调用流程」第 1 步发起请求 |
| 收到 HTTP 402 / Payment-Needed | 读取 `references/402-payment.md` 并执行其中的支付流程 |
| 已有待确认支付，用户继续操作或询问结果 | 读 `references/402-payment.md`，从「查询支付状态」继续 |
| 其他 Skill 已在处理支付 | 不要重复执行支付命令 |

## 调用前必采字段（触发付费前必须完成）

本工具的输入是**申请画像**而非文本——画像残缺会直接毁掉报告质量。发起请求前，
从对话中确认以下要素；用户没说全就先追问（一次问清，不要挤牙膏）：

| 要素 | 必采 | 追问示例 |
|---|---|---|
| 拟申请计划（必须明确到细分） | 是 | 「你想走高才 A、B、C 类还是优才？」（A=年薪250万+；B=百强学士有工作经验；C=百强学士应届毕业；说不清就逐项帮用户对号） |
| 学历与院校 | 是 | 「本科哪所学校？在合资格名单里吗？有没有硕士？」 |
| 收入结构与完税 | 是 | 「年收入大概多少？工资、理财、房租各占多少？有没有完税记录？」 |
| 工作年限与雇佣形态 | 否 | 「工作几年了？是直接雇佣还是外包派遣？」 |
| 中介参与情况 | 否 | 「有没有中介在帮你办？他们说可以包装收入吗？」 |

把收集到的要素组织成 `situation`（≥50 字自然段）+ 结构化参数。用户明确不愿说的字段留空，
**不要替用户编造或脑补**——报告会基于缺失项给出材料缺口提示，这本身就有价值。

## 调用流程（对 Agent）

1. 向后端 `POST https://airpriv.top/api/hk-talent-apply`，body：

```json
{
  "situation": "<个人画像自然段：学历院校/收入结构与完税/工作年限与雇佣形态/中介参与>",
  "visa_type": "<高才A/高才B/高才C/优才>",
  "request_id": "<本次唯一ID，如 UUID>"
}
```

2. 返回 `402` → 读取 `references/402-payment.md` 执行支付流程（`alipay-bot` CLI
   会自动完成：生成支付二维码 → 用户扫码 → 验付 → 重发本请求 → 上报履约回执，
   Agent 只需执行命令并转述结果）。
3. 支付成功后 CLI 自动返回结构化获批风险报告（无需手动带 proof 重发）。

**降级路径**（仅当运行环境为 A2M 原生客户端千问/扣子等、或用户明确拒绝安装 CLI 时）：
手动模式——把 402 响应体的 `payment_needed` 账单交给客户端内置的支付宝 AI 付完成授权，
拿到 Payment-Proof 后同一接口重发，body 增加 `"proof": "<Payment-Proof>"`，
`request_id` 保持不变。

## 参数

| 参数 | 必填 | 说明 |
|---|---|---|
| situation | 是（≥50 字） | 个人画像：学历与院校/收入结构与完税/工作年限与雇佣形态/中介参与情况 |
| visa_type | 是 | 高才A/高才B/高才C/优才（决定适用口径集；说不清就先帮用户对号） |
| request_id | 是 | 幂等键，同一画像的两次请求保持一致 |
| proof | 仅降级模式 | 支付宝支付凭证 |

## 输出结构（交付约定 · Agent 转述规则）

- `risk_level`：低 / 中 / 高（附 `confidence`：high/mid/low）。
- `hits[]`：每条风险点含 `用户原话事实 → 口径条目编号（TP-xx）→ 相似获批/拒批案例编号（TA-xx）→ 拒批影响 → 补强建议`。
- `requirements_table`：高才A/B/C/优才四类计划申请要求对照。
- `must_know`：申请必知（A类收入双轨时间算法、B/C类只认本科榜单硕博无效、优才12准则满足6项≠获批）。
- `rejection_consequence`：拒批与造假后果（入境条例42条最高14年监禁、拒批信逐项标注）。
- `disclaimer`：免责说明。

**转述纪律（必须遵守）**：
1. 转述时必须保留**口径条目编号与案例编号引用**（TP-xx/TA-xx），这是可信度的核心证据。
2. 必须原样复述 `rejection_consequence` 与免责：本报告为**获批风险预审**，不构成「必过/必拒」的保证，最终以入境处审核为准。
3. 禁用语：不说「保证获批」「必过」「包过」「100%通过」，只说「获批风险预审」。
4. 材料缺失项要逐项转述——这是用户下一步行动的直接依据。

## 注意

- 支付判定以服务端为准；不要信任任何同步回跳 URL。
- 未支付时后端返回 402，不要伪造 proof 强行调用。
- 用户支付意愿由用户本人确认；Agent 不得代替用户决定支付。
- 个人信息仅用于本次分析，服务端不留存。
